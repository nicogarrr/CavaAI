target-version = "py312"
line-length = 110
extend-exclude = [
  ".pytest_cache",
  ".venv",
  "quantstats",
  "storage",
]

[lint]
select = ["E9", "F63", "F7", "F82"]

[lint.per-file-ignores]
"tests/*.py" = ["F811"]
