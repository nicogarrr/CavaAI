# GENERADO — no editar a mano.
# Fuente unica: [project].dependencies de pyproject.toml.
# Regenerar/verificar:  python data-engine/scripts/sync_requirements.py --write | --check
# (el CI falla si derivan; ver job dep-parity en .github/workflows/ci.yml).
#
# torch CPU-only: el pin +cpu solo existe en el indice de PyTorch, de ahi el
# --extra-index-url. Los Dockerfiles pre-instalan el mismo pin desde ese
# indice para no depender del orden de resolucion. Jamas instalar torch de
# PyPI por defecto en este repo (wheels CUDA de GB en maquinas CPU-only).

--extra-index-url https://download.pytorch.org/whl/cpu

# Core API
fastapi>=0.139.0,<0.142
starlette>=1.3.1,<1.4
uvicorn[standard]>=0.29.0,<0.35
python-dotenv>=1.0.0,<2
python-multipart>=0.0.9,<0.1
pydantic>=2.7.0,<3
pydantic-settings>=2.4.0,<3

# HTTP
requests>=2.31.0,<3
httpx>=0.27.0,<0.29
# httpcore: public_fetch usa backends privados (IP pinning anti-SSRF);
# fijar la major para que un upgrade falle controlado, no en silencio.
httpcore>=1.0.0,<2.0

# Data / Finance providers
yfinance>=0.2.40,<0.3
pandas>=2.0.0,<3
lxml>=6.1.1,<7

# quantstats-pro dependencies (package vendored in ./quantstats/)
numpy>=1.24.0,<3
python-dateutil>=2.8.0,<3

# Knowledge base / RAG (embeddings locales all-MiniLM-L6-v2, gratis)
# torch: variante CPU-only (ver cabecera). transformers es transitiva via
# sentence-transformers, sin import directo en app/.
torch==2.9.1+cpu
qdrant-client>=1.9.0,<1.15
redis>=5.0.0,<6
sentence-transformers>=5.6.0,<6
transformers>=5.13.1,<6

# Document parsing
pypdf>=6.14.2,<7
openpyxl>=3.1.0,<4
python-docx>=1.1.0,<2

# Research OS backend
agent-framework-core==1.10.0
agent-framework-openai==1.10.0
agent-framework-orchestrations==1.0.0
sqlalchemy>=2.0.32,<2.1
alembic>=1.13.2,<1.16
psycopg[binary]>=3.2.1,<4
minio>=7.2.7,<8
dramatiq[redis]>=1.17.0,<2
apscheduler>=3.10.4,<4
langfuse>=3.0.0,<4
# LangGraph thesis lifecycle shadow pilot (stage 6): graph + durable checkpoints.
# Control state only in a separate schema; business source of truth stays in Postgres domain tables.
langgraph>=1.2.11,<2
langgraph-checkpoint-sqlite>=3.1,<4
langgraph-checkpoint-postgres>=3.1.2,<4
